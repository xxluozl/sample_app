<%= form_with(model: @micropost, local: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: '有什么新鲜事想分享给大家？' %>
  </div>
  <%= f.submit '发布', class: 'btn btn-primary' %>
<% end %>