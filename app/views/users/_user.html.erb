<li>
  <%= gravatar_for(user, size: 50) %>
  <%= link_to user.name, user_path(user) %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to '注销', user_path(user), method: :delete, data: { confirm: '确定注销吗？' } %>
  <% end %>
</li>